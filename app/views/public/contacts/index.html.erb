<h2 class="text-center mt-4"><%= image_tag "kumo.png" %><strong>お問い合わせ一覧</strong><%= image_tag "kumo.png" %></h2>
<div class="row">
  <div class="col-8 mx-auto">
    <div class="text-right">
      <%= link_to "新規問い合わせ", new_contact_path %>
    </div>
    <table class="table mt-4">
      <thead class="table-active">
        <tr>
          <th>タイトル</th>
          <th>内容</th>
          <th>対応状況</th>
          <th colspan="3"></th>
        </tr>
      </thead>
    <% @contacts.each do |contact| %>
      <tbody>
        <tr>
          <td><%= contact.title %></td>
          <td style="word-break : break-all;"><%= contact.body %></td>
          <td><%= contact.status %></td>
        </tr>
      </tbody>
    <% end %>
    </table>
  </div>
</div>